/**
 * @file Showcase page rendering the graph catalog previews.
 */

import type { ReactElement } from "react";
import { GraphCatalog } from "../components/graph/GraphCatalog";

export const GraphCatalogShowcase = (): ReactElement => {
  return <GraphCatalog />;
};
